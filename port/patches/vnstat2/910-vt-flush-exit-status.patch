Subject: vnstatd: report final database save failures to VT supervisor

Upstream 2.13 logs nonfatal SQLite errors while flushing at shutdown but
returns success. Preserve that error (and close failure) in the exit status,
so a configuration-backup caller cannot acknowledge an unsuccessful save.
Interface lookup step/finalize errors must reach the transaction rollback
path; a failed lookup must never be treated as an absent interface.
A private read-only capability option rejects unpatched installations.

Sources: https://github.com/vergoh/vnstat/blob/v2.13/src/vnstatd.c
         https://github.com/vergoh/vnstat/blob/v2.13/src/dbsql.c

--- a/src/vnstatd.c
+++ b/src/vnstatd.c
@@ -27,10 +27,16 @@
 
 int main(int argc, char *argv[])
 {
-	int currentarg;
+	int currentarg, flush_failed;
 	uint32_t previflisthash;
 	uint64_t temp;
 	DSTATE s;
+
+	/* Private supervisor capability: query without touching config or DB. */
+	if (argc == 2 && strcmp(argv[1], "--vt-flush-exit-status") == 0) {
+		puts("VT_VNSTAT_FLUSH_EXIT_V1");
+		return EXIT_SUCCESS;
+	}
 
 	initdstate(&s);
 
@@ -234,7 +240,11 @@
 	}
 
 	flushcachetodisk(&s);
-	db_close();
+	/* Preserve a nonfatal save error before later cleanup can change it. */
+	flush_failed = db_errcode != SQLITE_OK;
+	if (!db_close()) {
+		flush_failed = 1;
+	}
 
 	datacache_clear(&s.dcache);
 	ibwflush();
@@ -244,7 +254,7 @@
 		unlink(cfg.pidfile);
 	}
 
-	return 0;
+	return flush_failed ? EXIT_FAILURE : EXIT_SUCCESS;
 }
 
 void showhelp(void)
--- a/src/dbsql.c
+++ b/src/dbsql.c
@@ -436,7 +436,7 @@
 
 uint64_t db_getinterfacecountbyname(const char *iface)
 {
-	int rc;
+	int rc, final_rc;
 	uint64_t result = 0;
 	char sql[512], *inquery = NULL;
 	sqlite3_stmt *sqlstmt;
@@ -464,12 +464,27 @@
 		return 0;
 	}
 	if (sqlite3_column_count(sqlstmt) != 1) {
-		return 0;
-	}
-	if (sqlite3_step(sqlstmt) == SQLITE_ROW) {
-		result = (uint64_t)sqlite3_column_int64(sqlstmt, 0);
-	}
-	sqlite3_finalize(sqlstmt);
+		rc = SQLITE_ERROR;
+	} else {
+		rc = sqlite3_step(sqlstmt);
+		if (rc == SQLITE_ROW) {
+			result = (uint64_t)sqlite3_column_int64(sqlstmt, 0);
+			rc = SQLITE_OK;
+		} else if (rc == SQLITE_DONE) {
+			/* A count query must return one row, including a zero count. */
+			rc = SQLITE_ERROR;
+		}
+	}
+	final_rc = sqlite3_finalize(sqlstmt);
+	if (rc == SQLITE_OK) {
+		rc = final_rc;
+	}
+	if (rc != SQLITE_OK) {
+		db_errcode = rc;
+		snprintf(errorstring, 1024, "Failed to read interface count from database (%d): %s", rc, sqlite3_errmsg(db));
+		printe(PT_Error);
+		return 0;
+	}
 
 	/* consider merge query as invalid if not all requested interfaces are found or are not unique */
 	if (strchr(iface, '+') != NULL) {
@@ -483,22 +498,41 @@
 
 sqlite3_int64 db_getinterfaceid(const char *iface, const int createifnotfound)
 {
-	int rc;
+	int rc, final_rc;
 	char sql[128];
 	sqlite3_int64 ifaceid = 0;
 	sqlite3_stmt *sqlstmt;
 
 	sqlite3_snprintf(128, sql, "select id from interface where name='%q'", iface);
 	rc = sqlite3_prepare_v2(db, sql, -1, &sqlstmt, NULL);
+	if (rc != SQLITE_OK) {
+		db_errcode = rc;
+		snprintf(errorstring, 1024, "Failed to get interface id from database (\"name=\", %d): %s", rc, sqlite3_errmsg(db));
+		printe(PT_Error);
+		return 0;
+	}
+
+	if (sqlite3_column_count(sqlstmt) != 1) {
+		rc = SQLITE_ERROR;
+	} else {
+		rc = sqlite3_step(sqlstmt);
+		if (rc == SQLITE_ROW) {
+			ifaceid = sqlite3_column_int64(sqlstmt, 0);
+			rc = SQLITE_OK;
+		} else if (rc == SQLITE_DONE) {
+			/* Only a successful empty query may create a missing interface. */
+			rc = SQLITE_OK;
+		}
+	}
+	final_rc = sqlite3_finalize(sqlstmt);
 	if (rc == SQLITE_OK) {
-		if (sqlite3_step(sqlstmt) == SQLITE_ROW) {
-			ifaceid = sqlite3_column_int64(sqlstmt, 0);
-		}
-		sqlite3_finalize(sqlstmt);
-	} else {
-		db_errcode = rc;
-		snprintf(errorstring, 1024, "Failed to get interface id from database (\"name=\", %d): %s", rc, sqlite3_errmsg(db));
-		printe(PT_Error);
+		rc = final_rc;
+	}
+	if (rc != SQLITE_OK) {
+		db_errcode = rc;
+		snprintf(errorstring, 1024, "Failed to read interface id from database (\"name=\", %d): %s", rc, sqlite3_errmsg(db));
+		printe(PT_Error);
+		return 0;
 	}
 
 	if (ifaceid == 0 && createifnotfound) {
